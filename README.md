# Contenu
## Base
1. Comment crée des variables
    - Type de var
    - Intenger
    - String
    - Char
    - ...
2. Opérateurs de base (+ - * /)
3. Opérateurs avancé (%=>modulo)
## Structure 1
1. Opérateurs conditionnels
    - AND ou &&
    - OR ou ||
    - <>
    - <= >=
    - !=
    - ==
2. Bloc condition
    - if
    - else if
    - else
## Structure 2
1. Boucle
    - for
    - while
    - do-while
2. Pointeur (à placer a coté d'une variable)
    - *
    - &
## Fonction
1. Définir une fonction
    - void nom(type param){code}
    - int nom(type param){code; return int}
    - ...

# En cas de problème de compréhension du code
Si vous savez cree sur le répertoire Github une `issue` avec le code qui vous pose problème, j'essaierais au meilleur de mes capacités à vous aider
Si vous n'arrivez pas a creer une `issue`, je suis joinable par mail <a href="martin.biname@condorcet.be">martin.biname@condorcet.be</a>